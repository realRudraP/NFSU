#include<stdio.h>
void main(){
    int rate,avg_qty,no_people,amount;
    rate=430,avg_qty=5,no_people=15;
    amount= rate*avg_qty*no_people*30;
    printf("The total amount in one month is %d",amount);
    }
